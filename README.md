# track-system

track-system is an upstream awareness tool for package governance. It builds package tracking, metadata collection, diff analysis, version lifecycle identification, maintenance status evaluation, and report generation around the L0, L1, and L2 code relationship model.